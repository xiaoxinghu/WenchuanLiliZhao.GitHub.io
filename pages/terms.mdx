import Layout from '../components/layout/article'

export const metadata = {
  title: 'Terms and Conditions'
}

## some heading

This is written in markdown.

export default ({children}) => <Layout metadata={metadata}>{children}</Layout>
